cd /volume1/homes/ctctim14/樂程坊計畫/課程資料/Cursor/講師行事曆檢視/flb-calendar-nas && echo "🛑 停止容器..." && sudo docker-compose down && echo "" && echo "🚀 啟動容器（使用新配置）..." && sudo docker-compose up -d && echo "" && echo "⏳ 等待 5 秒..." && sleep 5 && echo "" && echo "🔍 檢查容器內部檔案..." && sudo docker-compose exec flb-calendar-nas ls -lh /app/public/admin-dashboard.html && echo "" && echo "📋 查看日誌..." && sudo docker-compose logs --tail=20 && echo "" && echo "✅ 測試訪問..." && curl -I https://calendar.funlearnbar.synology.me/admin-dashboard.html && echo "" && echo "🎉 完成！請在瀏覽器訪問: https://calendar.funlearnbar.synology.me/admin-dashboard.html"
