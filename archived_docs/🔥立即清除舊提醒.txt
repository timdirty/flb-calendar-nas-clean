╔══════════════════════════════════════════════════════════════╗
║   🔥 立即清除舊提醒並重新生成（正確時區）                   ║
╚══════════════════════════════════════════════════════════════╝

問題診斷
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

❌ 當前問題：
   顯示時間：2025-10-11 02:00
   正確時間：2025-10-11 10:00
   差距：8 小時（時區錯誤）

❌ 原因：
   舊的 reminders.json 包含用錯誤代碼生成的提醒
   scheduledTime 欄位是錯誤的 UTC 時間

✅ 解決方案：
   清除舊提醒 → 重啟服務 → 系統自動重新生成（使用新代碼）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚀 在 NAS 上執行（複製整段）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

cd /volume1/homes/ctctim14/樂程坊計畫/課程資料/Cursor/講師行事曆檢視/flb-calendar-nas && \
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━" && \
echo "🗑️  Step 1: 備份並清除舊提醒資料" && \
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━" && \
sudo docker exec flb-calendar-nas cp /app/data/reminders.json /app/data/reminders.json.backup.$(date +%Y%m%d_%H%M%S) 2>/dev/null || echo "⚠️ 無現有提醒檔案" && \
sudo docker exec flb-calendar-nas rm -f /app/data/reminders.json && \
echo "✅ 舊提醒已清除" && \
echo "" && \
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━" && \
echo "🔄 Step 2: 重啟 Docker 容器（載入新代碼）" && \
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━" && \
sudo docker-compose restart && \
echo "" && \
echo "⏳ 等待服務啟動並自動生成新提醒..." && \
sleep 30 && \
echo "" && \
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━" && \
echo "✅ Step 3: 驗證新提醒時間" && \
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━" && \
echo "" && \
echo "📝 檢查新生成的提醒（應該使用正確的台灣時間）：" && \
echo "" && \
sudo docker logs flb-calendar-nas --tail 100 | grep -E "⏰ 創建|提醒時間|台灣" | tail -20 && \
echo "" && \
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━" && \
echo "🔍 檢查 reminders.json 內容" && \
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━" && \
echo "" && \
sudo docker exec flb-calendar-nas cat /app/data/reminders.json 2>/dev/null | head -50 && \
echo "" && \
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━" && \
echo "🌐 立即檢查網頁" && \
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━" && \
echo "" && \
echo "https://calendar.funlearnbar.synology.me/course-reminder-management.html" && \
echo "" && \
echo "✅ 現在時間應該顯示為 10:00（而不是 02:00）" && \
echo ""

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

如果還是不對，手動觸發重新生成
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

方法 1: 在網頁上操作
  1. 打開 https://calendar.funlearnbar.synology.me/course-reminder-management.html
  2. 點擊「排程設定」標籤
  3. 點擊「立即執行」按鈕
  4. 系統會重新生成所有提醒

方法 2: 使用 API 觸發
  curl -X POST https://calendar.funlearnbar.synology.me/api/reminder-scheduler/run

方法 3: 重置特定類型的提醒
  # 重置當日提醒
  curl -X POST https://calendar.funlearnbar.synology.me/api/reminders/reset-today
  
  # 重置課前提醒（依行事曆）
  在網頁上點擊「重置課前提醒（依行事曆）」

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

預期結果
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

修復前：
  ESM 六 10:00-10:40 外 第3週
  講師: AGNES
  時間: 2025-10-11 02:00  ❌ 錯誤

修復後：
  ESM 六 10:00-10:40 外 第3週
  講師: AGNES
  時間: 2025-10-11 10:00  ✅ 正確

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

重要提醒
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚠️  清除舊提醒後，系統會在下次 midnight cleanup 或手動觸發時重新生成
⚠️  如果需要立即生成，請使用上面的「手動觸發」方法
⚠️  新生成的提醒將使用修復後的代碼，時間會是正確的台灣時間

立即執行上面的指令！

